This consists of 2 docker containers:
caddy
nginx

Once running, caddy reverse proxies the root domain to nginx from port 443 to port 80, which serves files from the nginx/data folder.


To install, install:
docker
docker-compose

Then run in the root directory:
docker-compose run
